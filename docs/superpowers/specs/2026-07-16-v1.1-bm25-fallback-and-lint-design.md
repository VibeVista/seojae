# v1.1 — BM25 Search Fallback + Deterministic Wiki Lint — Design Spec

- **Date:** 2026-07-16
- **Status:** Approved (brainstorm 2026-07-16, both sections approved as-is)
- **Scope source:** v1.1 brainstorm — themes A (onboarding friction) and
  C (wiki quality automation) selected; items A1 (BM25 fallback) and
  C1 (`tools/lint.py`) chosen; BM25 integration mode: **auto-fallback**
  (not an extension-slot replacement).

## Context

Seojae v1.0.0's biggest onboarding barrier is the search stack: installing
`chromadb` + `sentence-transformers` pulls in torch, and the first reindex
downloads a ~470MB embedding model. New users cannot try the wiki loop
(ingest → query) without minutes of setup.

Separately, the Lint workflow is entirely LLM-driven. Its mechanical checks
(broken wikilinks, orphans, frontmatter validity) are deterministic and should
not consume LLM effort — and cannot run in CI today.

## Goals

1. `git clone` → working search in seconds, with zero heavy dependencies.
2. A deterministic `tools/lint.py` usable by humans, the LLM Lint workflow,
   and CI.

## Non-goals

- Replacing the chromadb backend (it remains the recommended full experience).
- New extension-slot mechanics — the fallback lives inside `tools/search.py`;
  `extensions/search-chromadb.md` keeps `provides: search-backend` and its
  command tokens are unchanged.
- Semantic lint checks (stale info, missing pages) — those stay LLM-side.
- Morphological analysis for Korean — CJK bigram tokenization is sufficient
  at personal-wiki scale.

## Feature 1: BM25 auto-fallback search

### Behavior

- `tools/search.py` attempts `import chromadb` (lazy, as today). On
  `ImportError` it switches to the BM25 backend and prints one stderr notice:
  the active backend and the upgrade path
  (`pip install -r requirements.txt` + `--reindex`).
- New flag `--backend {auto,chroma,bm25}`, default `auto`. `chroma` without
  chromadb installed → error exit 1 with install hint. `bm25` forces BM25 even
  when chromadb is available (useful for tests and offline work).
- CLI contract is identical across backends **for single-collection use**:
  `--query/--add/--reindex/--top/--index-path/--wiki-path`, output
  `<path> [score: X.XX]`, exit codes 0 (success) / 1 (error) / 2 (index
  missing). WIKI_SCHEMA workflows and the search-chromadb extension require
  no command changes.
- **Multi-collection surface stays chromadb-only in v1.1**: `--collection`,
  `--collections`, and `--wiki-root` (connected-wikis machinery) require the
  chroma backend; using them under BM25 → exit 1 with a full-install hint.
  Detection caveat: `--collection` has a non-None argparse default (`"wiki"`),
  so the guard must be `args.collection != COLLECTION_NAME` (an explicitly
  non-default value), never mere presence/truthiness — otherwise every plain
  `--add`/`--reindex` call would exit 1 under BM25 and break the lite loop.
  `--collections`/`--wiki-root` default to None and are unambiguous.
  `--print-model` under BM25 reports `backend=bm25` with no embedding model.
  Under chroma, `--print-model` keeps today's exact output
  (`backend=search-chromadb` + model line) — it is asserted by
  `tests/test_search.py` and quoted verbatim in
  `docs/connected-wikis-guide.ko.md`, so it must not be renamed.
- **connected-wikis under lite install**: `tools/connected_wikis.py` already
  lazy-imports chromadb inside three functions (`_delete_collection`,
  `_collection_count`, `_indexed_files_missing`); with chromadb missing these
  raise a bare `ImportError` traceback mid-command. Each of those call sites
  (and any CLI entry point reaching them) must catch the missing package and
  exit 1 with the same install-hint message pattern as search.py.
  `extensions/connected-wikis.md` gains a stated requirement that connected
  wikis need the full (non-lite) install.

### Backend/index mismatch (`--backend auto`)

Package importability picks the backend (chromadb importable → chroma, else
BM25); index presence is then checked **against that backend's specific
artifact marker**, not the directory:

- Markers: chroma → its SQLite store inside `search-index/`
  (`chroma.sqlite3`); BM25 → `search-index/bm25.json`. The existing
  directory-level `Path(index_path).exists()` check in search.py must be
  rewritten to per-marker checks — otherwise a bm25-only run leaves
  `search-index/` present and a later chroma selection would silently
  `get_or_create_collection` an empty store and return zero results instead
  of exit 2.
- Chosen backend's marker missing → exit 2 with Reindex advice, exactly like
  today. This also covers the lite→full upgrade: after installing the full
  stack, the first query exits 2 and the schema's existing guidance runs
  Reindex, which rebuilds under chroma.
- `--reindex` under chroma additionally deletes a stale `bm25.json` if
  present (hygiene for the common lite→full path). The reverse (forcing
  `--backend bm25 --reindex` with chromadb installed) may leave both
  artifacts on disk — that is allowed and unambiguous, since detection is
  per-marker, never directory-level.

### Implementation

- Pure-Python BM25 Okapi (k1=1.5, b=0.75), no new dependencies. Lives in
  `tools/bm25.py` (importable unit), wired through `tools/search.py`.
- **IDF variant:** Lucene-style non-negative IDF,
  `log(1 + (N - n + 0.5) / (n + 0.5))` — avoids the classic Okapi negative
  scores for very common terms, keeping all scores ≥ 0.
- **Corpus statistics:** IDF and average document length are derived **at
  query time** from the full stored per-document data in `bm25.json` — never
  cached in the index — so `--add` upserts stay consistent without a corpus
  rebuild.
- **Tokenizer:** lowercase; Latin/numeric word tokens via regex; CJK
  characters additionally emitted as character bigrams (plus single chars for
  1-char runs). This gives practical Korean/Japanese/Chinese matching without
  a morphological analyzer.
- **Index:** JSON document at `search-index/bm25.json` (already gitignored):
  tokenized term frequencies per page, document lengths, page metadata
  (path, title). Rebuilt by `--reindex`; `--add` upserts a single page.
  Missing/corrupt index file → exit 2 (same semantics as the chroma path,
  triggering the schema's index.md fallback + Reindex advice).
- Indexed text = frontmatter title/tags/aliases + body, same
  frontmatter-required skip rule as the chroma backend.

### Score scale

BM25 raw scores are unbounded, but the Query workflow's "below 0.5 → also
scan index.md" rule assumes a bounded score. BM25 results are normalized
relative to the top hit (top result = 1.00) before output. Documents with a
raw score of 0 (no term matched) are excluded from results entirely, so the
top hit's score is always > 0 and the division is well-defined (with the
non-negative IDF variant, scores cannot be negative). Consequence: any
non-empty result set passes the threshold; an empty result set triggers the
existing fallback. Document this in the search-chromadb extension's
counterpart note and CLI reference.

### Install path

- `requirements.txt` (full stack) unchanged.
- New `requirements-lite.txt`: `PyYAML`, `pytest` only.
- README Quick Start and WIKI_SCHEMA Setup gain the two-tier path:
  lite install (seconds, BM25) now; upgrade to semantic later with
  `pip install -r requirements.txt` + Reindex.

## Feature 2: `tools/lint.py`

### Checks (all deterministic, no LLM)

| # | Check | Detail |
|---|-------|--------|
| 1 | Broken wikilinks | `[[X]]` in `wiki/` pages matching no page title, alias, or filename stem (`index.md` entries are covered by check 5, not this check) |
| 2 | Orphan pages | zero inbound wikilinks from other wiki pages (an `index.md` listing does not count as a link) |
| 3 | Frontmatter validity | parseable YAML; required fields `title/type/created/updated`; `type` ∈ entity/concept/source/synthesis; dates `YYYY-MM-DD`; `tags`/`aliases`/`sources` are lists |
| 4 | Filename uniqueness | stems unique across all `wiki/` subdirectories (Obsidian shortest-path rule) |
| 5 | index.md sync | wiki pages missing from `index.md`; `index.md` entries pointing at nonexistent pages |

Wikilink matching must mirror the search tool's page model: a link resolves
against page filenames (kebab-case stems), frontmatter `title`, and `aliases`.
Link-target extraction is a **new helper in lint.py** (do NOT reuse
`clean_wikilinks()` from `tools/search.py` — that function serves embedding
readability and does the opposite: it keeps the *display* text of
`[[Page|Display Text]]` and discards the page name). Lint's extractor keeps
the page-name part of `[[Page|Display Text]]` and strips the `#Heading`
anchor of `[[Page#Heading]]` before resolving — pipe/anchor links must not
be misreported as broken (check 1) or undercounted as inbound links
(check 2).

### Interface

- `python tools/lint.py [--wiki-path wiki/] [--index-md index.md] [--json]
  [--strict]`
  (flag named `--index-md`, not `--index-path`, to avoid collision with
  search.py's search-index directory flag, which both backends use)
- Default output: human-readable report grouped by check, one issue per line
  with `path: message`.
- `--json`: machine-readable list of `{check, path, message}` for the LLM
  workflow and CI annotations.
- Exit codes: `0` clean, `1` issues found, `2` usage error (bad path).
- Orphan-page findings are warnings by default (new wikis legitimately have
  few links); `--strict` promotes warnings to exit-code-1 issues. Checks 1,
  3, 4, 5 are always issues.

### Integration

- **WIKI_SCHEMA Lint workflow:** new step 1 — run `tools/lint.py --json`
  first; the LLM consumes its findings, then performs semantic checks
  (stale info, missing pages, cross-reference and growth suggestions).
  Renumber the remaining steps.
- **CI:** add a lint step to `.github/workflows/ci.yml` running
  `python tools/lint.py` against the repo's example wiki (keeps shipped
  example content healthy). Also add a **lite-install job** that installs
  only `requirements-lite.txt` and runs `test_bm25.py`, `test_lint.py`, and
  the connected-wikis lite-guard test in a genuinely chromadb-less
  environment — the unit-test import shim alone does not validate the
  "zero heavy deps" promise.
- **AGENTS.md:** mirror the WIKI_SCHEMA Lint workflow change (inlined copy).

## Error handling

- BM25 index corrupt/unreadable → exit 2 with Reindex advice (never crash).
- lint.py on a page with unparseable frontmatter reports it under check 3 and
  continues (one bad page must not abort the run).
- Both tools remain stdlib+PyYAML-only so the lite install always runs them.

## Testing

- `tests/test_bm25.py`: tokenizer (Latin, Korean bigram, mixed), scoring
  order, relative normalization, index round-trip, exit codes, `--backend`
  forcing, chromadb-absent simulation (import shim), output format
  `<path> [score: X.XX]`.
- `tests/test_lint.py`: fixtures per check (broken link, orphan, bad
  frontmatter, duplicate stem, index drift), `--json` shape, exit codes,
  `--strict`, pipe/anchor link extraction.
- `tests/test_connected_wikis.py` (extend existing file): chromadb
  ImportError → exit 1 with the shared install-hint message pattern.
- Backend-detection tests: per-marker detection (bm25.json present but
  chroma marker absent → exit 2 under chroma), stale `bm25.json` deleted by
  a chroma `--reindex`, multi-collection flags under BM25 → exit 1.
- Existing `tests/test_search.py` chroma-path assertions keep passing
  (extended with the marker/cleanup cases above where they fit best).
- CI benefit: BM25 and lint tests are model-free — they run fully under
  `SKIP_MODEL_TESTS=true`.

## Documentation & rollout

- README (en/ko): Quick Start gains the lite path; CLI reference gains
  `--backend` and score-scale note; Prerequisites soften the 470MB line
  ("optional, for semantic search"). Sweep both files for unconditional
  "semantic search" wording — e.g. the workflow table's Query row — and
  reword to backend-dependent ("semantic or BM25 keyword search, depending
  on install"). Also reword the CLI table's `--index-path` row, currently
  "ChromaDB index directory" (ko: "ChromaDB 인덱스 디렉토리") — the flag is
  backend-agnostic under the marker-file design; use "Search index
  directory". (This row lacks the words "semantic search", so the wording
  sweep alone would miss it.)
- docs/getting-started.md: Step 3 (currently a mandatory ~470MB model
  download) becomes the two-tier path — lite by default, semantic upgrade
  optional. Same "semantic search" sweep as README — e.g. Step 7 ("Query
  the Wiki") states semantic search as fact; reword to backend-dependent.
- landing/index.html: the workflow ledger table asserts "semantic search"
  unconditionally in two places — same backend-dependent rewording as the
  README sweep.
- extensions/search-chromadb.md: add the BM25 fallback counterpart note
  (score-scale difference, auto-detection, upgrade path). Its own CLI
  Reference table repeats the `--index-path` = "ChromaDB index directory"
  row — apply the same "Search index directory" rewording here (this file
  is imported verbatim into AGENTS.md, so the fix propagates to Codex
  users).
- extensions/connected-wikis.md: state the full-install requirement.
- CONTRIBUTING.md / CONTRIBUTING.ko.md: Development Setup gains the lite
  install path (`requirements-lite.txt`) and the lite test invocation
  (`test_bm25.py` / `test_lint.py` without chromadb), so contributors can
  reproduce the new lite-install CI job locally.
- WIKI_SCHEMA.md: Setup two-tier install; Lint workflow step 1;
  `schema_version` stays `1.0` (backward-compatible).
- AGENTS.md re-synced; check the 32KiB budget (`wc -c AGENTS.md`).
- CHANGELOG `[Unreleased]` → these two features; release as v1.1.0 per
  RELEASE_GUIDE.md after merge.
